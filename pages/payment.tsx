import type { NextPage } from "next";
import Layout from "../components/Layout";

const Home: NextPage = () => {
	return (
		<Layout>
			<span>is payment</span>
		</Layout>
	);
};

export default Home;
